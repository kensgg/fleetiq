import { Map } from 'lucide-react';
import { PlaceholderPage } from '@/components/dashboard/PlaceholderPage';

export default function RutasPage() {
  return (
    <PlaceholderPage
      icon={Map}
      title="Rutas"
      description="Planifica y monitorea rutas: asigna camiones y conductores, rastrea el progreso y gestiona incidencias."
    />
  );
}
